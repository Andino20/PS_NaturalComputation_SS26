# Baseline Results

This directory contains generated benchmark results.

The current result table was generated with the dependency-free reference implementation in `run_benchmark.py`.

Run the benchmark from the repository root:

```bash
python3 run_benchmark.py
```

The script trains each benchmark problem once per hidden-layer activation function and writes the final metrics to `baseline_results.csv`.
