# PS Natural Computation - AI Basics

Benchmark problems and baseline rules for comparing activation functions in artificial neural networks.

See [BENCHMARK.md](BENCHMARK.md) for the common benchmark definition.

Install the dataset generation dependencies with:

```bash
pip install -r requirements.txt
```
